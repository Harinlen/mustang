// @ts-nocheck
import { Container, Text } from "@/tui/index.js";
export class AgentDashboard extends Container {
  constructor(..._args) { super(); this.addChild(new Text("AgentDashboard unavailable in Mustang ACP mode", 1, 0)); }
  dispose() {}
}
