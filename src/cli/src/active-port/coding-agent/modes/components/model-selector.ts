// @ts-nocheck
import { Container, Text } from "@oh-my-pi/pi-tui";
export class ModelSelectorComponent extends Container {
  constructor(..._args) { super(); this.addChild(new Text("ModelSelectorComponent unavailable in Mustang ACP mode", 1, 0)); }
  dispose() {}
}
