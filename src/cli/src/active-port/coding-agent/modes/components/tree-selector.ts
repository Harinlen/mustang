// @ts-nocheck
import { Container, Text } from "@oh-my-pi/pi-tui";
export class TreeSelectorComponent extends Container {
  constructor(..._args) { super(); this.addChild(new Text("TreeSelectorComponent unavailable in Mustang ACP mode", 1, 0)); }
  dispose() {}
}
