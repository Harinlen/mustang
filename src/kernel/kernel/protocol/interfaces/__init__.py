"""Protocol-agnostic interface layer.

Contains the abstract contracts (Python ``typing.Protocol`` classes)
and Pydantic data contracts that define the boundary between the
protocol layer and the session layer.  Nothing in this package depends
on ACP wire-format details.
"""
